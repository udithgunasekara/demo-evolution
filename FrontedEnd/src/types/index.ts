export interface Post {
      _id: string;
      title: string;
      description: string;
      titleColor: string;
    }
    
    export interface Comment {
      _id: string;
      content: string;
    }